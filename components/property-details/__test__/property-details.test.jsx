import { render, screen } from '@testing-library/react'

it('should display the correct address and price in the property details card', () => {
  // render(<PropertyDetails property={propertyMock} />)
  // const priceElement = screen.getByText('$500,000')
  // const addressElement = screen.getByText('123 Main St')
  // expect(priceElement).toBeInTheDocument()
  // expect(addressElement).toBeInTheDocument()
})
